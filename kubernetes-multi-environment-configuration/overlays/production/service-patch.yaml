apiVersion: v1
kind: Service
metadata:
  name: web-app-service

spec:
  type: ClusterIP

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80

    - name: https-simulated
      protocol: TCP
      port: 443
      targetPort: 80
