apiVersion: v1
kind: Service
metadata:
  name: sample-app-service
  namespace: deployment-strategies
  labels:
    app: sample-app
spec:
  type: ClusterIP

  selector:
    app: sample-app
    version: v1

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
