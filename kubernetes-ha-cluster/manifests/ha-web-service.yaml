apiVersion: v1
kind: Service
metadata:
  name: ha-web
  namespace: default
spec:
  selector:
    app: ha-web
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
