apiVersion: platform.example/v1
kind: WebApp
metadata:
  name: my-web-application
  namespace: default
spec:
  replicas: 3
  image: nginx:1.27-alpine
  port: 80

  env:
    - name: ENV_TYPE
      value: production

    - name: LOG_LEVEL
      value: info
