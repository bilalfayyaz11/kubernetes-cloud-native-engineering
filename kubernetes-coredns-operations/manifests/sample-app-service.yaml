apiVersion: v1
kind: Service
metadata:
  name: sample-web-service
  namespace: default
  labels:
    app: sample-web-app
spec:
  type: ClusterIP
  selector:
    app: sample-web-app
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 80
