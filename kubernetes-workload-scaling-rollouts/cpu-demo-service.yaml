apiVersion: v1
kind: Service
metadata:
  name: cpu-demo-service
spec:
  selector:
    app: cpu-demo
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP
