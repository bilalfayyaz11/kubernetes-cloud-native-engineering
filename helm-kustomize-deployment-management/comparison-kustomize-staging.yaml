apiVersion: v1
kind: Service
metadata:
  labels:
    environment: staging
    managed-by: kustomize
  name: webapp-service
  namespace: staging
spec:
  ports:
  - name: http
    nodePort: 30081
    port: 80
    protocol: TCP
    targetPort: http
  selector:
    app: webapp
  type: NodePort
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    environment: staging
    managed-by: kustomize
  name: webapp
  namespace: staging
spec:
  replicas: 2
  selector:
    matchLabels:
      app: webapp
  template:
    metadata:
      labels:
        app: webapp
        environment: staging
        managed-by: kustomize
    spec:
      containers:
      - env:
        - name: ENVIRONMENT
          value: staging
        image: nginx:1.28-alpine
        livenessProbe:
          httpGet:
            path: /
            port: http
          initialDelaySeconds: 10
          periodSeconds: 10
        name: webapp
        ports:
        - containerPort: 80
          name: http
        readinessProbe:
          httpGet:
            path: /
            port: http
          initialDelaySeconds: 2
          periodSeconds: 5
        resources:
          limits:
            cpu: 100m
            memory: 128Mi
          requests:
            cpu: 50m
            memory: 64Mi
