apiVersion: v1
kind: Service
metadata:
  name: my-webapp-service
  namespace: default
  labels:
    app: my-webapp
spec:
  selector:
    app: my-webapp
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
