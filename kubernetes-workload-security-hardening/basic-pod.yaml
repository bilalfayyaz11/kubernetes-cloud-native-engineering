apiVersion: v1
kind: Pod
metadata:
  name: basic-pod
  labels:
    app: security-demo
spec:
  restartPolicy: Never
  containers:
    - name: demo-container
      image: nginx:1.29-alpine
      command:
        - /bin/sh
        - -c
      args:
        - sleep 3600
