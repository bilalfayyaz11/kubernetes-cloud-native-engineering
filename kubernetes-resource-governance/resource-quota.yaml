apiVersion: v1
kind: ResourceQuota
metadata:
  name: compute-quota
  namespace: resource-governance
spec:
  hard:
    requests.cpu: "1"
    requests.memory: 1Gi
    limits.cpu: "2"
    limits.memory: 2Gi
    persistentvolumeclaims: "4"
    pods: "10"
    services: "5"
