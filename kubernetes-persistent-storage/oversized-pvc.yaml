apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: oversized-pvc
  namespace: persistent-storage
spec:
  storageClassName: manual
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
