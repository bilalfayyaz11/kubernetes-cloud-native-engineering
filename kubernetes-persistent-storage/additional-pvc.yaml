apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: secondary-pvc
  namespace: persistent-storage
spec:
  storageClassName: manual
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
