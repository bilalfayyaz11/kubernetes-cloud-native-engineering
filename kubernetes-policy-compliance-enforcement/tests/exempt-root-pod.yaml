apiVersion: v1
kind: Pod
metadata:
  name: exempt-root
  namespace: test-exempt
spec:
  containers:
  - name: busybox
    image: busybox:latest
    command:
    - sh
    - -c
    - sleep 3600
    securityContext:
      runAsUser: 0
      privileged: false
