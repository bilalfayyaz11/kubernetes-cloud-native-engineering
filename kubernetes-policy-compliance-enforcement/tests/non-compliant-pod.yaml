apiVersion: v1
kind: Pod
metadata:
  name: non-compliant
  namespace: default
spec:
  containers:
  - name: nginx
    image: nginx:latest
    securityContext:
      runAsUser: 0
      privileged: true
